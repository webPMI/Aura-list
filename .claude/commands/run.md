# /run - Run Flutter App

Run the checklist app in debug mode.

## Arguments
- `$ARGUMENTS` - Device: `chrome`, `windows`, `edge`, or device ID (optional)

## Instructions

1. Check available devices with `flutter devices`
2. Run `flutter run -d $ARGUMENTS` if argument provided, else `flutter run`
3. Monitor output for errors and suggest fixes if needed
